{
  "name": "sonority",
  "description": "Audio visualizer webapp made of ES2015 and WebGL",
  "dependencies": {
    "three": "^0.76.1",
    "three-orbit-controls": "^72.0.0",
    "three-loaders-collada": "^1.0.1"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-concat": "^2.6.0",
    "jsdoc": "^3.4.0",
    "live-server": "^1.0.0",
    "three": "^0.76.1",
    "three-collada-loader": "0.0.1",
    "three-loaders-collada": "^1.0.1",
    "three-orbit-controls": "^72.0.0",
    "vinyl-source-stream": "^1.1.0"
  },
  "scripts": {
    "dev": "bash -c \" npm run build && live-server --port=8848 ./dist/ & gulp watch \"",
    "build": "npm run clean && gulp && jsdoc -c jsdoc.json",
    "clean": "rm -rf ./dist ./doc",
    "release": "NODE_ENV=production npm run build"
  },
  "private": true,
  "license": "GPL-3.0",
  "version": "1.0.0"
}
